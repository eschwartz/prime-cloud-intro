{
  "name": "prime-cloud-intro",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "HOSTNAME=$(curl http://checkip.amazonaws.com) PORT=80 node ./server.js",
    "server": "sudo PORT=80 forever start -a --uid todo server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.7",
    "express": "^4.17.1",
    "forever": "^4.0.3"
  }
}
